version: "2"

services:
  web:
    container_name: hubspot-shopper-app-web
    environment:
      NODE_ENV: development
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./src:/app/src
    ports:
      - 3000:3000
    command: ["npm", "start"]
